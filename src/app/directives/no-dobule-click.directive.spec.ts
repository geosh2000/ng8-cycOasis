import { NoDobuleClickDirective } from './no-dobule-click.directive';

describe('NoDobuleClickDirective', () => {
  it('should create an instance', () => {
    const directive = new NoDobuleClickDirective();
    expect(directive).toBeTruthy();
  });
});
